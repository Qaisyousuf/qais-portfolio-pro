# Allow all search engines to crawl the site
User-agent: *
Allow: /

# Disallow crawling of API endpoints
Disallow: /api/

# Sitemap location
Sitemap: https://www.getrok.com/sitemap.xml

# Crawl delay (optional, helps prevent rate limiting)
Crawl-delay: 1